#include<iostream>
#include<vector>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int> dsk;
    vector<int> kln;
    vector<int> pr;
    int cd,ckl;
    for(int i = 0; i < n; ++i){
        int temp;
        cin >> temp;
        dsk.push_back(temp);
    }
    for(int i = 0; i < n; ++i){
        int temp;
        cin >> temp;
        kln.push_back(temp);
    }
    for(int i = 0; i < n; ++i){
        
    }
}