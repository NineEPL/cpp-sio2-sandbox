#include<bits/stdc++.h>
using namespace std;
using ll = long long;
const ll duzo = 1e9;

int main(){
/*         ll n, k;
        cin >> k >> n;
        vector<ll> dp(n+1,duzo);
        vector<ll> monety;
        for(ll i = 0; i < n; ++i){
            ll temp;
            cin >> temp;
            monety.push_back(temp);
        }
        dp[0] = 0;
        for(ll i = 0; i < n; ++i){
            for(ll j = monety[i]; j <= k; ++j){
                dp[j] = min(dp[j], dp[j - monety[i]] + 1);

            }
        }
        cout << dp[dp.size() - 1];
         */
        cout << "I'm the storm that is approaching\n";
}